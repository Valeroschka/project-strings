spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/guitar_db
    username: postgres
    password: secret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  security:
    user:
      name: admin
      password: secret123